name: generic_app
description: A generic mobile app built on the App Factory platform.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_ui:
    path: ../../packages/shared_ui
  shared_database:
    path: ../../packages/shared_database
  flutter_riverpod: ^2.5.1
  riverpod: ^2.5.1
  google_fonts: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
