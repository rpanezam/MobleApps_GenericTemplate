// Minimal smoke test so 'flutter test' (melos run test) passes for this package in CI.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('shared_ads smoke test', () {
    expect(true, isTrue);
  });
}
